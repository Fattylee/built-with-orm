const sumAll = (a, b) => a + b;
const subtractAll = (a, b) => a - b;
const divideAll = (a, b) => a / b;
const multiplyAll = (a, b) => a * b;
export default {
  sumAll,
  subtractAll,
  divideAll,
  multiplyAll,
};
